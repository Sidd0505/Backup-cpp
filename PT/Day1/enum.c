#include<stdio.h>
enum Days{Monday, Tuesday, Wed};
int main()
{
    enum Days dd;

    switch(Monday)
    {
        case Monday :
            printf("mondayyy");
            break;
        default :
            printf("Today.");
    }
}