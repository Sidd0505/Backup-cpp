#include <stdio.h>

int main()
{
    for (int choice = 1; choice != 6; choice++)
    {
        int distance;
        printf("Enter the distance in km :");
        scanf("%d", &distance);
        
    }
}